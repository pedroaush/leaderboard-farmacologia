[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm run build"

[deploy]
startCommand = "npx drizzle-kit push --force && node dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
