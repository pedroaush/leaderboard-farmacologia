[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm run build"

[deploy]
startCommand = "npx drizzle-kit migrate && node dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
